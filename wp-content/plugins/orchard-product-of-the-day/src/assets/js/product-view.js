(($) => {
    $(() => {
        // TODO: (OPTIONAL) - Save the data associated with the user's click on the Call to Action (CTA) along with the current date and time into a custom table.
    });
}) (jQuery);